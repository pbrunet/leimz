package com.map;


/*
public class Region 
{
	private int idRegion;
    private ArrayList<City> cities;
    private EntitiesManager entities_manager;
    private String name;

    public List<Joueur> getClientNear(Tile tile)
    {
    	List<Joueur> listeClient = new ArrayList<Client>();
    	for( Joueur cli : entities_manager.getClients_manager())
    	{
    		if(cli.getCompte().getCurrent_joueur().isNear(cli.act.t,tile))
    		{
    			listeClient.add(cli);
    		}
    	}
    	return listeClient;
	}

    public void sendToClient(String mess) {
	for(Client cli : listClient)
	{
		cli.sendToClient(mess);
	}
	
}*/
